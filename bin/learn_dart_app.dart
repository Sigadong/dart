
void main() {
  bool alwaysTrue = true;
  var alwaysFalse = false;

  // Sign ! above is called the "not" or "bang" operator.
  // This operator is used to negate boolean values
  var notTrue = !true;
  bool notFalse = !false;
}